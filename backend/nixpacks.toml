# Railway / Nixpacks: keep Node provider packages and add ffmpeg for back-video extraction.
# See https://nixpacks.com/docs/configuration/file (array extending with "...")
[phases.setup]
nixPkgs = ["...", "ffmpeg"]
